{
    "name": "limb/session",
    "description": "PHP's session wrapper. This package conveniently wraps PHP's session with OO interface. It also provides support for transparent session objects persistence.",
    "require": {
        "php": ">=7.2",
        "limb/core": "^4.2",
        "limb/dbal": "^4.2"
    },
    "autoload": {
        "psr-0": {
            "limb\\session\\": ""
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev"
}
